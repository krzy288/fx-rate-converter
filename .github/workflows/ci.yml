name: CI Pipeline

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]


jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      working-directory: backend
      run: | 
        python -m pip install --upgrade pip 
        pip install -r requirements.txt

    - name: Run basic FastAPI checkout
      working-directory: backend
      run: |
        python -c "import main; print('✅ App module loaded successfully')" 

    - name: Build docker image
      run: |
        docker build -t fx-converter .

    - name: Run docker container 
      run: | 
        docker run -d -p 8000:8000 --name fx-app fx-converter

    - name: Test if app is live (basic check)
      run: |
        sleep 3
        curl -f http://localhost:8000/docs