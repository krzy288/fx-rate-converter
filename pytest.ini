# pytest.ini
[pytest]
pythonpath = .
markers =
    playwright: mark a test as using Playwright

# Playwright configuration for different modes
addopts = 
    # Default headless mode
    # Add --headed for visual debugging
    # Add --slowmo=1000 for slow motion
    # Add --browser-channel=chrome for Chrome instead of Chromium